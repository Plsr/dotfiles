" General setup
filetype plugin on "requierd by netwr

set relativenumber " set relative line numbers

" Use two spaces for tabs
set tabstop=2
set shiftwidth=2
set expandtab

" netwr
let g:netrw_banner = 0 " We don't want no banner
let g:netrw_liststyle = 3 " Use tree style listing
